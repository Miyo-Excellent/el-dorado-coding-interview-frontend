✨ fix(docs): Sustituir video por GIF hipercomprimido en el README

## 📝 Resumen General
> [!NOTE]
> Debido a las limitantes del formato Markdown en la renderización directa de video nativo (`.mp4`), se ha transcodificado el video original a un formato `.gif` altamente comprimido utilizando FFMPEG y mapeo de paleta de colores.

## 🛠️ Cambios Realizados
- [x] **Archivo Multimedia:** Eliminación del archivo `demo.mp4`.
- [x] **Archivo Multimedia:** Generación y adición de `demo.gif` optimizado a 15 frames por segundo, garantizando una alta retención visual pero reduciendo el peso en ~75%.
- [x] **Documentación:** Actualizada la inserción en el `README.md` volviendo al tag `<img src="...">` correspondiente.

### Tabla Orientativa
| Archivo | Acción | Motivo |
|---------|---------|---------|
| `demo.mp4` | Remove | Incompatibilidad base con visores Markdown |
| `demo.gif` | Add | GIF generado por terminal, <40MB permitiendo push sin conflictos |
| `README.md` | Refactor | Reversión de los tags de video a imagen |

## 📐 Arquitectura Afectada (Mermaid)
```mermaid
graph LR;
    A(WebM) -->|FFMPEG <br> palettegen| B[Generar <br> Paleta Optima];
    A --> C{Filtergraph};
    B --> C;
    C -->|Render| D(GIF Comprimido);
```

## 🐛 Bug Fixes
- Resuelto el problema provocado por el reproductor predeterminado del visor de Markdown y superadas las limitaciones de Push de GitHub de 100MB gracias al proceso de sub-muestreo inteligente.
